# planner

Use Codex Plan mode directly.

Read requirements and current repo state. Produce a decision-complete plan in `workflow/state/plan.md`.

Each task must include:

- `id`
- `title`
- `goal`
- `inputs`
- `outputs`
- `acceptance`
- `depends_on`
- `status`

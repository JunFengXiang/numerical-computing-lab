---
status: unverified_ocr_candidate
pdf_page: 81
source_image: ../../source-images/pdf-081.jpeg
---

> 未经逐页校对的 OCR 候选；数学符号和图表不得直接用于推导或绘图。

[查看原页](../../source-images/pdf-081.jpeg)

个数, 故 $y = F(t)$ 有一水平渐近线. 另外, $t = 0$ 时, 反应未开始, 质量分数为零. 根据这些特点, 可设想 $y = F(t)$ 是双曲线型 $1/y = a + b/t$, 即 $y = t/(at + b)$. 它与给定数据的规律大致符合.

为了确定 $a, b$, 令 $\bar{y} = 1/y, x = 1/t$, 于是可用 $x$ 的线性函数 $S_1(x) = a + bx$ 拟合数据 $(x_i, \bar{y}_i)$ ($i = 1, 2, \cdots, 16$), $x_i, \bar{y}_i$ 由原始数据 $(t_i, y_i)$ 根据变换计算出来. 与例 3.5 方法相同, 解方程组

$$\begin{cases} 16a + 3.380 \times 73b = 1.837 \times 10^3, \\ 3.380 \times 73a + 1.584 \times 35b = 0.528 \times 86 \times 10^3, \end{cases}$$

得 $a = 80.6621, b = 161.6822$.

从而得到

$$y = t/(80.6621t + 161.6822) = F^{(1)}(t),$$

其误差为

$$\delta_i^{(1)} = y_i - F^{(1)}(t_i) \quad (i = 1, 2, \cdots, 16).$$

由图 3.7, 符合给定数据的函数还可选为指数形式. 此时可令拟合曲线形如

$$y = a \mathrm{e}^{b/t}.$$

显然, 当 $t \to \infty$ 时, $y \to a$; 当 $t \to 0$ 时, 若 $b < 0$, 则 $y \to 0$, 且 $t$ 增加时 $y$ 增加. 这些与给出数据规律相同. 为了确定 $a$ 与 $b$, 对上式两端取对数, 得 $\ln y = \ln a + b/t$. 令

$$\hat{y} = \ln y, \quad A = \ln a, \quad x = 1/t,$$

于是由 $(t_i, y_i)$ 计算出 $(x_i, \hat{y}_i)$, 拟合数据 $(x_i, y_i)$ 的曲线仍为 $S_1(x) = A + bx$.

用例 3.5 的方法计算出 $A = -4.48072, b = -1.0567$, 从而

$$a = \mathrm{e}^{A} = 11.325 \times 10^{-3},$$

最后求得

$$y = 11.325 \times 10^{-3} \mathrm{e}^{-1.0567t} = F^{(2)}(t),$$

误差为

$$\delta_i^{(2)} = y_i - F^{(2)}(t_i) \quad (i = 1, 2, \cdots, 16).$$

怎样比较这两个数学模型的好坏呢? 只要分别计算各点误差, 从中挑选误差较小的模型即可. 本例经过计算可得

$$\|\delta^{(1)}\|_{\infty} = \max_i |\delta_i^{(1)}| = 0.568 \times 10^{-3},$$

$$\|\delta^{(2)}\|_{\infty} = \max_i |\delta_i^{(2)}| = 0.277 \times 10^{-3},$$

而均方误差为

$$\|\delta^{(1)}\|_2 = \sqrt{\sum_i^m (\delta_i^{(1)})^2} = 1.19 \times 10^{-3},$$

$$\|\delta^{(2)}\|_2 = \sqrt{\sum_i^m (\delta_i^{(2)})^2} = 0.34 \times 10^{-3}.$$
